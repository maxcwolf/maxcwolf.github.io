//requires each page's HTML.js file in this folder
const landingHTML = require("./landingHTML")

const innerHTML = [landingHTML]//add each HTML object from the requires above here

module.exports = innerHTML